<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= typeof title !== 'undefined' ? title : 'GreatTime Bot — панель' %></title>
  <link rel="icon" type="image/png" href="/logo.png" />
  <link rel="apple-touch-icon" href="/logo.png" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <header class="topbar">
    <a class="brand" href="/">
      <img class="brand-logo" src="/logo.png" alt="GT" onerror="var p=this.parentNode; if(p) p.classList.add('no-logo'); this.remove();" />
      GreatTime
    </a>
    <nav>
      <a href="/commands">Команды</a>
      <a href="/bot">О боте</a>
      <% if (inviteUrl) { %>
        <a class="btn ghost" href="<%= inviteUrl %>" target="_blank" rel="noopener">Добавить бота</a>
      <% } %>
      <% if (user) { %>
        <a href="/dashboard">Мои серверы</a>
        <span class="me">
          <%= user.global_name || user.username %>
        </span>
        <a class="btn ghost" href="/logout">Выйти</a>
      <% } else { %>
        <a class="btn" href="/login">Войти через Discord</a>
      <% } %>
    </nav>
  </header>
  <main class="container">
